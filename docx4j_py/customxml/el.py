"""``el`` for the http://schemas.openxmlformats.org/schemaLibrary/2006/main namespace.

Generated by ``codegen/generate_el.py`` from the ``XmlContext`` class metadata;
do not edit. The naming rules, the collisions and the class each name resolves
to are in ``codegen/el_tables/customxml.json``, which a human reviews.

    >>> from docx4j_py.customxml import el
    >>> el.schema()  # doctest: +SKIP

Every function takes the dataclass's own keyword arguments and passes them
straight through. A class that carries text (``w:t``, ``w:delText``,
``w:instrText``: anything whose metadata has a text value field) also takes the
text as the one positional argument, and sets ``xml:space="preserve"`` when the
text begins or ends with whitespace or holds two in a row, which is what
docx4j's ``t()`` does.
"""

from __future__ import annotations

from typing import Any

from docx4j_py.runtime import text_element

from docx4j_py.customxml import (
    SchemaLibrary,
    SchemaLibrarySchema,
)

__NAMESPACE__ = "http://schemas.openxmlformats.org/schemaLibrary/2006/main"

def schema(**kwargs: Any) -> SchemaLibrarySchema:
    """``{http://schemas.openxmlformats.org/schemaLibrary/2006/main}schema`` -> :class:`docx4j_py.customxml.SchemaLibrarySchema`."""
    return SchemaLibrarySchema(**kwargs)

def schemaLibrary(**kwargs: Any) -> SchemaLibrary:
    """``{http://schemas.openxmlformats.org/schemaLibrary/2006/main}schemaLibrary`` -> :class:`docx4j_py.customxml.SchemaLibrary`."""
    return SchemaLibrary(**kwargs)

#: Element qualified name -> the class ``el.<name>`` builds.
QNAME_TO_CLASS: dict[str, type] = {
    "{http://schemas.openxmlformats.org/schemaLibrary/2006/main}schema": SchemaLibrarySchema,
    "{http://schemas.openxmlformats.org/schemaLibrary/2006/main}schemaLibrary": SchemaLibrary,
}

#: Local element name -> the class ``el.<name>`` builds.
ELEMENTS: dict[str, type] = {
    "schema": SchemaLibrarySchema,
    "schemaLibrary": SchemaLibrary,
}

#: Class -> the one element qualified name it stands for, where there is one.
CLASS_TO_QNAME: dict[type, str] = {
    SchemaLibrary: "{http://schemas.openxmlformats.org/schemaLibrary/2006/main}schemaLibrary",
    SchemaLibrarySchema: "{http://schemas.openxmlformats.org/schemaLibrary/2006/main}schema",
}

__all__ = [
    "CLASS_TO_QNAME",
    "ELEMENTS",
    "QNAME_TO_CLASS",
    "schema",
    "schemaLibrary",
]
