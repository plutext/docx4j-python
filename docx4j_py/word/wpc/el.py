"""``el`` for the http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas namespace.

Generated by ``codegen/generate_el.py`` from the ``XmlContext`` class metadata;
do not edit. The naming rules, the collisions and the class each name resolves
to are in ``codegen/el_tables/word_wpc.json``, which a human reviews.

    >>> from docx4j_py.word.wpc import el
    >>> el.bg()  # doctest: +SKIP

Every function takes the dataclass's own keyword arguments and passes them
straight through. A class that carries text (``w:t``, ``w:delText``,
``w:instrText``: anything whose metadata has a text value field) also takes the
text as the one positional argument, and sets ``xml:space="preserve"`` when the
text begins or ends with whitespace or holds two in a row, which is what
docx4j's ``t()`` does.
"""

from __future__ import annotations

from typing import Any

from docx4j_py.runtime import text_element

from docx4j_py.dml.main import (
    CTBackgroundFormatting,
    CTOfficeArtExtensionList,
    CTWholeE2OFormatting,
)
from docx4j_py.word.wpc import (
    Wpc,
)
from docx4j_py.word.wpg import (
    CTGraphicFrame,
)

__NAMESPACE__ = "http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas"

def bg(**kwargs: Any) -> CTBackgroundFormatting:
    """``{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}bg`` -> :class:`docx4j_py.dml.main.CTBackgroundFormatting`."""
    return CTBackgroundFormatting(**kwargs)

def extLst(**kwargs: Any) -> CTOfficeArtExtensionList:
    """``{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}extLst`` -> :class:`docx4j_py.dml.main.CTOfficeArtExtensionList`."""
    return CTOfficeArtExtensionList(**kwargs)

def graphicFrame(**kwargs: Any) -> CTGraphicFrame:
    """``{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}graphicFrame`` -> :class:`docx4j_py.word.wpg.CTGraphicFrame`."""
    return CTGraphicFrame(**kwargs)

def whole(**kwargs: Any) -> CTWholeE2OFormatting:
    """``{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}whole`` -> :class:`docx4j_py.dml.main.CTWholeE2OFormatting`."""
    return CTWholeE2OFormatting(**kwargs)

def wpc(**kwargs: Any) -> Wpc:
    """``{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}wpc`` -> :class:`docx4j_py.word.wpc.Wpc`."""
    return Wpc(**kwargs)

#: Element qualified name -> the class ``el.<name>`` builds.
QNAME_TO_CLASS: dict[str, type] = {
    "{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}bg": CTBackgroundFormatting,
    "{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}extLst": CTOfficeArtExtensionList,
    "{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}graphicFrame": CTGraphicFrame,
    "{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}whole": CTWholeE2OFormatting,
    "{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}wpc": Wpc,
}

#: Local element name -> the class ``el.<name>`` builds.
ELEMENTS: dict[str, type] = {
    "bg": CTBackgroundFormatting,
    "extLst": CTOfficeArtExtensionList,
    "graphicFrame": CTGraphicFrame,
    "whole": CTWholeE2OFormatting,
    "wpc": Wpc,
}

#: Class -> the one element qualified name it stands for, where there is one.
CLASS_TO_QNAME: dict[type, str] = {
    CTBackgroundFormatting: "{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}bg",
    CTGraphicFrame: "{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}graphicFrame",
    CTOfficeArtExtensionList: "{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}extLst",
    CTWholeE2OFormatting: "{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}whole",
    Wpc: "{http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas}wpc",
}

__all__ = [
    "CLASS_TO_QNAME",
    "ELEMENTS",
    "QNAME_TO_CLASS",
    "bg",
    "extLst",
    "graphicFrame",
    "whole",
    "wpc",
]
