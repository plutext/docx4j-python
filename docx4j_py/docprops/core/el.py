"""``el`` for the http://schemas.openxmlformats.org/package/2006/metadata/core-properties namespace.

Generated by ``codegen/generate_el.py`` from the ``XmlContext`` class metadata;
do not edit. The naming rules, the collisions and the class each name resolves
to are in ``codegen/el_tables/docprops_core.json``, which a human reviews.

    >>> from docx4j_py.docprops.core import el
    >>> el.coreProperties()  # doctest: +SKIP

Every function takes the dataclass's own keyword arguments and passes them
straight through. A class that carries text (``w:t``, ``w:delText``,
``w:instrText``: anything whose metadata has a text value field) also takes the
text as the one positional argument, and sets ``xml:space="preserve"`` when the
text begins or ends with whitespace or holds two in a row, which is what
docx4j's ``t()`` does.
"""

from __future__ import annotations

from typing import Any

from docx4j_py.runtime import text_element

from docx4j_py.docprops.core import (
    CoreProperties,
)

__NAMESPACE__ = "http://schemas.openxmlformats.org/package/2006/metadata/core-properties"

def coreProperties(**kwargs: Any) -> CoreProperties:
    """``{http://schemas.openxmlformats.org/package/2006/metadata/core-properties}coreProperties`` -> :class:`docx4j_py.docprops.core.CoreProperties`."""
    return CoreProperties(**kwargs)

#: Element qualified name -> the class ``el.<name>`` builds.
QNAME_TO_CLASS: dict[str, type] = {
    "{http://schemas.openxmlformats.org/package/2006/metadata/core-properties}coreProperties": CoreProperties,
}

#: Local element name -> the class ``el.<name>`` builds.
ELEMENTS: dict[str, type] = {
    "coreProperties": CoreProperties,
}

#: Class -> the one element qualified name it stands for, where there is one.
CLASS_TO_QNAME: dict[type, str] = {
    CoreProperties: "{http://schemas.openxmlformats.org/package/2006/metadata/core-properties}coreProperties",
}

__all__ = [
    "CLASS_TO_QNAME",
    "ELEMENTS",
    "QNAME_TO_CLASS",
    "coreProperties",
]
