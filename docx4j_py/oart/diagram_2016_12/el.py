"""``el`` for the http://schemas.microsoft.com/office/drawing/2016/12/diagram namespace.

Generated by ``codegen/generate_el.py`` from the ``XmlContext`` class metadata;
do not edit. The naming rules, the collisions and the class each name resolves
to are in ``codegen/el_tables/oart_diagram_2016_12.json``, which a human reviews.

    >>> from docx4j_py.oart.diagram_2016_12 import el
    >>> el.lstStyle()  # doctest: +SKIP

Every function takes the dataclass's own keyword arguments and passes them
straight through. A class that carries text (``w:t``, ``w:delText``,
``w:instrText``: anything whose metadata has a text value field) also takes the
text as the one positional argument, and sets ``xml:space="preserve"`` when the
text begins or ends with whitespace or holds two in a row, which is what
docx4j's ``t()`` does.
"""

from __future__ import annotations

from typing import Any

from docx4j_py.runtime import text_element

from docx4j_py.oart.diagram_2016_12 import (
    LstStyle,
    SpPr,
)

__NAMESPACE__ = "http://schemas.microsoft.com/office/drawing/2016/12/diagram"

def lstStyle(**kwargs: Any) -> LstStyle:
    """``{http://schemas.microsoft.com/office/drawing/2016/12/diagram}lstStyle`` -> :class:`docx4j_py.oart.diagram_2016_12.LstStyle`."""
    return LstStyle(**kwargs)

def spPr(**kwargs: Any) -> SpPr:
    """``{http://schemas.microsoft.com/office/drawing/2016/12/diagram}spPr`` -> :class:`docx4j_py.oart.diagram_2016_12.SpPr`."""
    return SpPr(**kwargs)

#: Element qualified name -> the class ``el.<name>`` builds.
QNAME_TO_CLASS: dict[str, type] = {
    "{http://schemas.microsoft.com/office/drawing/2016/12/diagram}lstStyle": LstStyle,
    "{http://schemas.microsoft.com/office/drawing/2016/12/diagram}spPr": SpPr,
}

#: Local element name -> the class ``el.<name>`` builds.
ELEMENTS: dict[str, type] = {
    "lstStyle": LstStyle,
    "spPr": SpPr,
}

#: Class -> the one element qualified name it stands for, where there is one.
CLASS_TO_QNAME: dict[type, str] = {
    LstStyle: "{http://schemas.microsoft.com/office/drawing/2016/12/diagram}lstStyle",
    SpPr: "{http://schemas.microsoft.com/office/drawing/2016/12/diagram}spPr",
}

__all__ = [
    "CLASS_TO_QNAME",
    "ELEMENTS",
    "QNAME_TO_CLASS",
    "lstStyle",
    "spPr",
]
